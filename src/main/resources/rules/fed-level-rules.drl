import com.wf.rules.domain.CustomRules;
global com.wf.rules.domain.RulesList rulesList;

dialect "mvel"

rule "Throttle Value based rule"
	when
		CustomRules(throttleValue)
	then
		rulesList.setList("Throttling is On")
end